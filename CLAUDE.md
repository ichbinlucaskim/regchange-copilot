# CLAUDE.md — RegChange Copilot 작업 규약

이 문서는 이 저장소에서 코드를 작성하는 모든 주체(사람, 에이전트)에게 적용되는 구속력 있는
규약이다. 여기 적힌 규칙과 충돌하는 코드는 리뷰에서 반려된다.

---

## 0. 이 시스템은 무엇인가

한국 금융 규제의 법령 개정을 **조문 단위**로 포착하여, 사내 내부통제 규정 중 영향받는
문단을 **근거 인용과 함께** 제안하는 시스템이다.

**대상 도메인은 정보보호·전자금융이다.** 1차 사용자는 **정보보호부(CISO 조직)의 규제대응
담당자**다. 도메인은 감이 아니라 12개월 개정 빈도 실측으로 골랐다 —
`docs/domain-selection/amendment-frequency.md`, 근거는 ADR-008.

**컴플라이언스 담당자의 명시적 승인 없이는 어떤 정책 변경도, 티켓 생성도, 외부 발송도
실행되지 않는다.** 이 문장은 마케팅 문구가 아니라 아키텍처 제약이며, 코드로 강제된다.

---

## 제품 철학 — 선택의 순간이 아니라 선택에 이르는 과정

이 시스템은 의사결정을 대신하지 않는다. **결정에 이르는 과정을 지원한다.**

| 단계 | 시스템이 하는 일 |
|---|---|
| **읽고** | 수집 · 파싱 · 검색 · 근거 추출 |
| **보고** | 영향평가 초안, 기안 문서 생성 |
| **결정** | 승인 게이트, 결재선, 이력 |

이것은 마케팅 문구가 아니라 **설계 제약**이다. 아래 결정들이 여기서 도출된다:

- 조문 이동을 자동 확정하지 않는다 (ADR-003)
- 근거가 부족하면 답을 만들지 않고 이관한다
- 별표를 억지로 분석하지 않고 "미분석"으로 보고한다 (ADR-004)
- 승인 게이트를 UI가 아니라 그래프 레벨에 둔다 (원칙 4)

새 기능을 설계할 때 물어라: **"이것은 과정을 지원하는가, 선택을 대신하는가?"**
후자라면 다시 설계한다.

---

## 이 프로젝트가 증명하는 것 — 3축

| 축 | 질문 |
|---|---|
| **축 1 (업무)** | 누구의 어떤 업무를, 어떤 실패를 막으며 돕는가 |
| **축 2 (환경)** | 은행 IT 제약(망분리 · 클라우드 심의 · 전자결재 · 직무분리) 안에서 굴러가는가 |
| **축 3 (운영)** | 혼자서 실제로 몇 달간 운영해 봤는가 |

**셋 다 있어야 한다.** 코드를 쓸 때 어느 축에 기여하는지 의식하라. 어느 축에도 기여하지
않는 코드는 지금 쓸 코드가 아니다.

---

## 1. 핵심 설계 원칙 6가지

모든 구현 결정은 아래 6개 원칙에 비추어 정당화될 수 있어야 한다. 원칙을 위반하는 코드는
"동작하니까 괜찮다"로 통과시키지 않는다.

### 원칙 1 — 조문 diff는 결정론적 코드로 처리한다

LLM은 "무엇이 바뀌었나"에 쓰지 않는다. "그것이 무엇을 의미하나"에만 쓴다.

무엇이 바뀌었는지는 사실 확인이지 추론이 아니다. 원문 두 버전을 비교하는 일에 LLM을 넣으면
정확도는 떨어지고 비용과 비결정성만 늘어난다. 텍스트 비교·구조 비교·변경유형 분류는 전부
순수 함수로 작성하고, 같은 입력에 항상 같은 출력을 내야 한다.

LLM에 넘기는 것은 diff의 **결과**이며, LLM은 그 결과를 해석만 한다.

### 원칙 2 — 인용은 출력 스키마의 필수 필드다

모델 출력 스키마에서 인용(citation)은 optional이 아니다. 그리고 인용된 문단 ID가 실제
검색 결과 집합에 존재하지 않으면 **코드가 응답 전체를 폐기한다.** 경고 로그를 남기고
통과시키는 것이 아니라 폐기한다.

근거 없는 제안은 근거 있는 제안보다 나쁘다. 담당자가 검증할 수 없는 문장을 그럴듯하게
읽히는 형태로 내놓는 것은 이 도메인에서 실패 모드다.

### 원칙 3 — 초안 생성 노드와 인용 검증 노드를 분리한다

같은 모델 호출이 초안을 쓰고 그 초안을 검증하면 검증은 형식화된다. 생성기는 검증기의
프롬프트를 보지 않고, 검증기는 생성기의 추론 과정을 보지 않는다. 검증기는 오직
**출력 텍스트와 검색된 원문**만 받아 대조한다.

검증기는 "이 인용이 이 주장을 뒷받침하는가"에만 답한다. 문장을 고쳐 쓰지 않는다.

### 원칙 4 — 인간 승인은 그래프 레벨에 둔다

승인은 UI 레이어의 검사가 아니라 LangGraph의 `interrupt`로 구현한다. UI 검사는 API를
직접 호출하면 우회된다. 그래프가 중단되어 있으면 우회 경로 자체가 존재하지 않는다.

승인 없이 다음 노드로 진행하는 코드 경로를 만들지 않는다. "테스트 편의를 위한 우회
플래그"도 만들지 않는다. 테스트는 승인 이벤트를 실제로 주입해서 통과시킨다.

### 원칙 5 — LLM 프로세스는 DB 읽기 전용 role을 쓴다

LLM이 관여하는 모든 프로세스는 읽기 전용 DB role로 접속한다. 쓰기와 외부 발송은 **별도
워커**가 수행하며, 그 워커는 프롬프트나 모델 출력을 보지 않고 **승인 레코드만** 보고
동작한다.

프롬프트 인젝션이 성립하더라도 쓰기 권한이 없으면 피해가 조회로 제한된다. 이 경계는
애플리케이션 코드의 조건문이 아니라 **DB 권한**으로 강제한다. 코드로 강제한 경계는
코드 수정으로 무너지지만 권한으로 강제한 경계는 그렇지 않다.

### 원칙 6 — bitemporal 모델을 쓴다

모든 규제·정책 레코드는 `valid_time`(현실에서 효력이 있었던 기간)과
`transaction_time`(시스템이 그 사실을 알고 있었던 기간)을 함께 가진다.

감사 대응의 질문은 "지금 무엇이 맞는가"가 아니라 **"그 시점에 담당자가 알 수 있었던
정보는 무엇이었는가"**이다. 레코드를 덮어쓰면 이 질문에 답할 수 없다. 어떤 테이블에서도
`UPDATE`로 과거를 지우지 않는다.

---

## 2. 이 시스템이 하지 않는 일

아래는 기능 미구현 목록이 아니라 **의도적인 범위 제외**다. 이 항목들을 구현하자는 제안은
설계 결정의 번복이므로 ADR 없이 진행하지 않는다.

1. **침해사고를 탐지하거나 대응하지 않는다.** 보안관제(SOC)의 일이다.
2. **취약점 스캔·모의해킹을 하지 않는다.** 보안기술팀 또는 외부 전문기관의 일이다.
3. **개인정보를 열람하거나 처리하지 않는다.** 기능 미구현이 아니라 **설계상 배제**다.
   이 시스템이 다루는 것은 법령 원문과 사내 규정 문서이며, 개인정보는 입력에 없다.
4. **사내 정책을 자동으로 수정·배포하지 않는다.** 초안을 만들고 반영은 기안·결재 절차가 한다.
5. **감독당국이나 정보주체에게 자동 통지하지 않는다.** CISO/CPO 이상의 결재 사항이다.
6. **법률 자문에 해당하는 해석을 제공하지 않는다.** 금감원 질의, 법무팀, 외부 자문의 영역이다.
   출력은 검토 보조 자료이지 법적 판단이 아니다.

---

## 3. 필수 조항 — 4항목 docstring

**이 프로젝트에서 구현되는 모든 아키텍처 구성요소, 모듈, 클래스, 그리고 자명하지 않은
모든 함수에는 docstring에 아래 4가지를 반드시 명시한다. 하나라도 빠지면 미완성으로 본다.**

| 항목 | 무엇을 쓰는가 |
|---|---|
| **목적** | 이 함수/구성요소가 무엇을 하는가 |
| **구현 이유** | 왜 이 방식을 택했는가. 대안 대비 무엇이 나은가 |
| **트레이드오프** | 무엇을 포기했는가. 포기한 것이 없다고 생각되면 "없음"이라 쓰지 말고 **왜 없는지**를 쓴다 |
| **엣지 케이스** | 어떤 예외 상황을 상정했고 각각을 어떻게 처리하는가 |

### 왜 이 규칙이 있는가

이 시스템의 출력은 규제 대응 근거로 쓰인다. 6개월 뒤 감사에서 "왜 이렇게 판단했는가"를
물었을 때 답할 수 있어야 한다. 코드가 무엇을 하는지는 코드를 읽으면 알 수 있지만,
**무엇을 포기했는지와 무엇을 상정했는지는 작성자만 안다.** 그것이 사라지면 이후의 수정자는
의도적 설계를 버그로 오인하고 "고쳐서" 원칙을 무너뜨린다.

특히 **트레이드오프**와 **엣지 케이스**가 이 규칙의 핵심이다. 목적과 구현 이유만 쓰인
docstring은 규칙을 지킨 것이 아니다.

### 형식 예시 (이 형식을 그대로 따른다)

```python
def compute_article_diff(before: Article | None, after: Article | None) -> ArticleDiff:
    """조문 두 버전 사이의 구조적 차분을 계산한다.

    목적:
        법령 개정 전후 조문을 비교해 변경 유형(신설/개정/삭제/조번호이동)과
        변경된 텍스트 범위를 산출한다.

    구현 이유:
        LLM에게 "무엇이 바뀌었는지" 묻지 않는다. 법제처가 조문별 변경이력을
        제공하므로 이는 사실 확인이지 추론이 아니며, LLM을 쓰면 정확도를
        낮추고 비용만 늘린다. LLM은 이 함수의 출력을 받아 "그 변화가 무엇을
        의미하는가"만 해석한다.

    트레이드오프:
        텍스트 유사도 기반이므로 조문 전체가 재작성된 경우 "삭제 + 신설"로
        보고될 수 있다. 의미적 연속성 판단을 포기한 대신 결정론성과 재현성을
        얻었다. 재현성이 감사 요구사항이므로 이 교환은 의도적이다.

    엣지 케이스:
        - 조번호 이동(제5조 → 제5조의2): path 변경으로 감지, RENUMBERED 반환
        - before가 None: 신설 조문으로 간주, ADDED 반환
        - after가 None: 삭제 조문으로 간주, DELETED 반환
        - 공백/줄바꿈만 다른 경우: 정규화 후 비교하여 EDITORIAL로 분류
        - 텍스트가 빈 문자열: ValueError를 던진다. 파서 버그를 숨기지 않는다
    """
```

### 모듈 docstring

모듈(`__init__.py` 포함)도 같은 4항목 구조를 따르되, 함수가 아니라 **패키지의 역할**을
기술한다. "이 패키지가 무엇을 담당하는가 / 왜 별도 경계로 분리했는가 / 그 분리로 무엇을
포기했는가 / 이 경계에서 어떤 실패를 상정하는가".

### 자명한 함수의 예외

`__repr__`, 단순 property getter, 한 줄짜리 위임 함수는 한 줄 docstring으로 갈음할 수
있다. 판단 기준은 **"이 함수에 대해 트레이드오프를 쓸 것이 있는가"**이다. 있다면 자명하지
않은 것이므로 4항목을 쓴다. 애매하면 쓴다.

### 아키텍처 수준 결정

아키텍처 수준의 결정은 docstring이 아니라 `docs/05-architecture-decisions/` 아래 ADR
파일로 남긴다. ADR도 **동일한 4항목 구조**를 따른다. 템플릿은 `adr-000-template.md`.

docstring과 ADR의 경계: 한 모듈 안에서 끝나는 결정은 docstring, 두 개 이상의 모듈이나
운영·보안 경계에 영향을 주는 결정은 ADR.

---

## 4. 코드 규약

- **Python 3.12**
- **ruff + mypy strict.** 둘 다 CI 게이트이며 경고가 아니라 에러다.
- **모든 함수에 타입 힌트 필수.** 반환 타입 포함. `Any`는 외부 경계에서만 허용하고,
  쓸 때는 왜 좁힐 수 없는지 docstring에 적는다.
- **예외를 삼키지 않는다.** `except Exception: pass`와 조용한 기본값 반환을 금지한다.
  잡았으면 (a) 더 구체적인 예외로 변환해 다시 던지거나 (b) 구조화 로그에 컨텍스트를 남기고
  호출자에게 실패를 알린다. 규제 도메인에서 조용한 실패는 오답보다 나쁘다.
- **매직 넘버 금지.** 임계값·타임아웃·top-k·유사도 컷오프는 이름 있는 상수로 정의하고,
  그 값을 왜 그 값으로 정했는지 주석 또는 docstring에 남긴다. 근거 없는 상수는 리뷰에서
  반려한다.
- **실패 모드를 먼저 세라.** 함수가 실패·부재할 수 있는 방식이 둘 이상이면 **반환
  타입으로 구별한다.** `None`·`bool`·빈 컬렉션으로 여러 상태를 표현하지 않는다.
  특히 **호출부가 세거나 분기하는 값**, **지표나 기록에 들어가는 값**이 그렇다.
  이 원칙은 도메인 코드에서는 지켜졌으나 **경계 코드(저장·측정·러너)에서 네 번
  무너졌다** — `docs/19-state-conflation-audit.md`.
- **운영 결과를 확인할 때는 운영이 쓰는 함수를 부른다.** 파서 결과나 DB 행의 필드를
  직접 읽지 않는다. 측정 러너뿐 아니라 **리뷰도 테스트도 거치지 않는 일회성 조사
  코드**에도 적용되며, 후자가 더 위험하다 — 우회하기 쉽고 틀려도 드러나지 않는다.
  이 패턴으로 다섯 번 틀렸다. 마지막은 `assemble_body()` 대신 `조문내용` 을 직접 읽어
  **「본문이 오지 않는다」는 없는 발견**을 만든 것이다 —
  `docs/incidents/measurement-reported-failure-as-success.md` §5-1.
- **새 상수나 임계값을 도입하면 `docs/17-engineering-knobs.md` 에 등재한다.** 근거를
  **측정 / 추론 / 관행 / 설계**로 구별해 적는다. **근거가 없으면 상수를 만들지 않는다** —
  `UNSUPPORTED` 임계를 만들지 않은 것, 발화하지 않는 정합성 규칙 7개를 `ENFORCED` 로
  올리지 않은 것, 이동 점수 가중치를 동결한 것이 전부 같은 규칙의 적용이다.
- **문서와 docstring은 한국어**로 쓴다. 식별자·타입명·커밋 메시지는 영어.
- **순수 함수를 기본값으로 한다.** I/O는 adapters 경계로 밀어낸다. diff·temporal·
  verification 은 I/O 없이 테스트 가능해야 한다.

---

## 5. 금지 사항

### 5.1 확인하지 않은 규제 내용을 쓰지 않는다

**확인하지 않은 법령 조항 번호나 규제 문구를 코드·주석·문서·테스트 픽스처에 쓰지 않는다.**

LLM은 그럴듯한 조항 번호를 만들어낸다. 사람도 기억에 의존하면 틀린다. 이 저장소의 문서는
컴플라이언스 담당자가 읽는 문서이므로, 틀린 조항 번호 하나가 신뢰 전체를 무너뜨린다.

불확실하면 이렇게 표시한다:

```python
# TODO(verify): 조항 번호 확인 필요 — 법제처 원문 대조 후 채울 것
ARTICLE_REF = "TODO(verify)"
```

```markdown
<!-- TODO(verify): 조항 번호 미확인. 원문 확인 전까지 채우지 말 것 -->
```

그리고 **사람에게 확인을 요청한다.** 자리를 비워두는 것은 허용되지만 지어내는 것은 허용되지
않는다. `make lint` 이전에 `TODO(verify)`가 남아 있는 것은 정상이며, 배포 전에 해소한다.

### 5.2 그 외 금지

- 승인 절차를 우회하는 코드 경로 (테스트 전용 플래그 포함)
- 쓰기 권한을 가진 커넥션을 LLM 프로세스에 주입하는 것
- 과거 레코드의 `UPDATE`/`DELETE` (bitemporal 위반)
- 실제 고객 데이터를 테스트 픽스처나 eval 데이터셋에 넣는 것
- `.env`, 자격증명, 실 API 키를 커밋하는 것

---

## 6. 테스트 규약

- 테스트는 `tests/unit`, `tests/integration`, `tests/security` 로 나눈다.
- **`tests/security` 는 반드시 유지한다.** 리팩터링 편의를 이유로 삭제하거나 skip 하지
  않는다. 최소한 아래를 계속 검사한다:
  - LLM용 DB role로 `INSERT`/`UPDATE`/`DELETE` 를 시도하면 **실패**해야 한다
  - 킬 스위치(`LLM_ENABLED`, `RETRIEVAL_ENABLED`, `DISPATCH_ENABLED`)가 꺼진 상태에서
    해당 기능이 **실행되지 않아야** 한다
  - 킬 스위치의 기본값은 **꺼짐**이어야 한다 (fail-closed)
  - 승인 레코드 없이 dispatch 워커가 동작하지 않아야 한다
- 결정론적 로직(diff, temporal)은 **단위 테스트로 고정**한다. 성질 기반 테스트를 권장한다.
- LLM 호출은 단위 테스트에서 스텁으로 대체한다. 실제 호출은 `evals/` 에서만 한다.
- 테스트 파일에는 4항목 docstring을 강제하지 않는다. 다만 **왜 이 테스트가 존재하는지**
  한 줄은 남긴다. security 테스트는 어떤 위협을 막는지 반드시 명시한다.

---

## 7. 커밋 규약

[Conventional Commits](https://www.conventionalcommits.org/) 를 따른다.

```
<type>(<scope>): <subject>
```

- **type**: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `perf`, `build`, `ci`
- **scope**: 패키지명 (`ingest`, `diff`, `graph`, `guards`, `dispatch`, …)
- **subject**: 영어, 명령형, 마침표 없음

예시:

```
feat(diff): add deterministic article-level diff
fix(guards): reject citations missing from retrieval set
docs(adr): record read-only DB role decision
```

설계 원칙에 영향을 주는 변경은 본문에 관련 ADR 번호를 적는다 (`Refs: ADR-004`).

---

## 8. 저장소 구조

```
src/regchange/
├─ adapters/      외부 시스템 경계 (storage / queue / llm / secrets) — Protocol 정의
├─ ingest/        법령 원문 수집·정규화
├─ parse/         조문 XML 파싱과 텍스트 정규화     [ADR-001, ADR-002]
├─ store/         bitemporal 스키마·적재·시점 질의  [원칙 6]
├─ diff/          조문 단위 결정론적 차분          [원칙 1]
├─ temporal/      bitemporal 모델과 시점 재구성    [원칙 6]
├─ retrieval/     사내 규정 문단 검색
├─ graph/         LangGraph 그래프·상태·승인 interrupt [원칙 4, ADR-018]
├─ prompts/       프롬프트 정의 (생성기/검증기 분리) [원칙 3]
├─ verification/  인용 검증 — 생성과 분리된 경로    [원칙 2, 3]
├─ guards/        킬 스위치, 스키마 강제, 인용 폐기 [원칙 2, 5]
├─ review/        검토 큐·승인 레코드·반려 사유       [원칙 4, ADR-018]
├─ api/           FastAPI 진입점
├─ dispatch/      승인 레코드만 보고 동작하는 발송 워커 [원칙 5]
├─ ops/           일일 작업·실행 이력·조회 명령        [ADR-014]
└─ audit/         감사 로그와 재현
```

- `diff`, `temporal`, `verification` 은 I/O 의존성을 갖지 않는다.
- `pipeline` 은 **한 건을 어떻게 처리하는가**를, `ops` 는 **매일 무엇을 얼마나 도는가**를
  조립한다. 스케줄·재확인 창·실패 격리·실행 이력은 후자다.
- `dispatch` 는 `graph`, `prompts`, `llm` 어댑터를 import 하지 않는다. 이 방향 의존이
  생기면 원칙 5가 무너진 것이다.
- `review` 는 `prompts` 를 import 하지 않는다. 초안을 `ImpactDraft` 가 아니라 **사전(JSON)**
  으로 다룬다 — 검토 큐가 프롬프트 스키마를 알면 `dispatch` 가 나중에 이 패키지를 쓸 때
  프롬프트를 간접 import 하게 된다.
- `verification` 은 `prompts` 도 import 하지 않는다. 프롬프트 모듈이 `adapters.llm` 의 타입을
  쓰므로 그것을 거치면 I/O 격리 계약이 **간접 경로로** 깨진다. 판정 어휘(`SupportLevel`)는
  `verification` 이 정의하고 프롬프트가 가져다 쓴다.

---

## 9. 개발 명령

```bash
make setup      # uv sync — 의존성 설치
make up         # docker compose up -d (Postgres + pgvector)
make down       # docker compose down
make migrate    # db/migrations 적용 (적용된 것은 건너뛴다)
make lint       # ruff check
make fmt        # ruff format + --fix
make typecheck  # mypy strict
make test       # pytest

make review-ui     # 검토 UI (127.0.0.1:8000) — 승인은 그래프 재개로만 이루어진다
make eval-impact   # 영향평가 + gate 3단 골든셋 측정 (실제 모델을 부른다)
make eval-delegation # 위임 승격 top_n 스윕 (R-22)
make eval-routing-precheck # 라우팅 사전 확인 — 기록과 검색만 읽는다 (LLM 미호출, $0)
make eval-baseline # B-1/B-2/B-3 대조 — 같은 채점 함수 (LLM 미호출, $0)
make eval-citation-adequacy # 인용 적합도 판정지 생성 — 판정은 사람이 한다 (LLM 미호출, $0)
make eval-citation-adequacy-score # 채워진 판정지 → F-6 과 신뢰구간 (LLM 미호출, $0)
make eval-power    # 검정력 분석 — 지금까지의 대조가 유의한가 (LLM 미호출, $0)

make ops-run       # 일일 작업 1회 (cron 과 같은 경로)
make ops-install   # launchd 등록 (매일 07:00 KST)
make ops-summary   # 운영 실적 — 실행일 / 실패일 / 미실행일 / 총 포착

regchange switch list      # 킬 스위치 현재 값 + 마지막 변경 사유 (ADR-019)
regchange switch history   # 누가 언제 왜 바꿨는가 — 감사 질문의 답
regchange switch off LLM_ENABLED --by <사람> --reason "<왜>"   # --by/--reason 필수
```

**킬 스위치는 `.env` 가 아니라 DB 에 있다.** 반영은 **최대 60초**이며 즉시 필요하면
프로세스를 재시작한다. 기본값은 꺼짐이다 — 행이 없으면 꺼진 것으로 읽는다.

---

## 10. 현재 단계

**수집 · 파싱 · 적재 · 차분이 구현됐고, 2026-08-20부터 매일 자동 실행된다** (ADR-014).
**4단계에서 LangGraph 와 승인 게이트가, 5단계에서 신뢰 등급과 킬 스위치가 들어왔다.**
아래는 아직 의도적으로 비어 있다:

- 발송 워커에서의 `DISPATCH_ENABLED` 검증 (R-25) — 관문만 있고 부를 워커가 없다
- **사내 부서 마스터** — 부서 배정이 자유 텍스트라 조직도와 대조되지 않는다 (R-24)
- 인젝션 **차단** 로직 — 지금도 탐지·기록만 한다. 무엇을 차단할지는 적대적 입력을 본 뒤에
- 발송 워커 (`dispatch/`) — 계약과 권한 경계만 있다
- **「그 인용을 했어야 하는가」를 재는 척도** — F-6 사람 판정은 끝났으나(20건, 0.0000)
  **그 척도가 case-013 을 통과시켰다.** 빈칸이 「값이 없다」에서 **「재는 방법이 없다」**
  로 옮겨졌다 (`docs/23` §2.8)
- 결재선 (`approval_line`) — 단일 승인자로 시작한다 (ADR-018)
- Terraform 코드
- 실제 규제 조항 번호와 법령 문구 (원문 미확인)

채워진 것 (각각 실측 또는 픽스처 채점과 함께 들어왔다):

- 법제처 API 호출 코드 — `ingest/`
- 조문 파서 — `parse/`, 픽스처 채점 정확도 1.0000
- DB 스키마와 적재 — `db/migrations/`, `store/`. 조문 `valid_from` 은 일자별 이력
  API 결합 전까지 NULL(`PENDING_HISTORY`)이며, 본문의 조문시행일자를 승격하지 않는다
  (edge-case #8, ADR-005)
- 결정론적 조문 diff — `diff/`, `store/changeset.py`. I/O 의존 없음 (원칙 1)
- 일일 운영 — `ops/`. **실패한 실행도 행으로 남긴다** — "실패한 날"과 "실행하지 않은
  날"은 다른 사실이며 조치도 다르다. 실적 주장의 근거는 로그가 아니라 질의다
- 평가 코퍼스 — `evals/corpus/internal-policies/` 5종 152조(합성). 골든셋 15건과의 대조
  결과는 `docs/09-corpus-verification.md`, 참조 무결성은 `tests/unit/test_policy_corpus.py`
- 사내 규정 검색 — `retrieval/`, `db/migrations/009`. 조 단위 청킹, 시점 파라미터,
  벡터+어휘 하이브리드(ADR-016), 로컬 임베딩 KURE-v1(ADR-015). **채점 기준을 측정
  이전에 고정했다** — `docs/10-retrieval-evaluation-protocol.md`, 결과는 `-results.md`.
  재현율@10 0.7667, EASY 1.0000. 정책 계층 조항이 안 잡히는 것은 R-22
- 의무사항 추출과 근거 강제 gate — `prompts/`, `guards/`, `pipeline/obligations.py`.
  **gate 2단은 순수 코드다** — 인용 문단 ID 와 인용문을 검색 결과와 대조해 폐기하며
  LLM 에게 묻지 않는다. 최종 `INSUFFICIENT_EVIDENCE` 판정도 모델이 아니라 코드가 한다
- LLM 호출 기록 — `audit/invocation.py`, `db/migrations/010`. **첫 호출부터** 남기며
  실패·재시도도 행으로 남는다 (ADR-013). `temperature`/`seed` 컬럼은 없다 —
  대상 모델이 거부한다 (`docs/incidents/llm-api-schema-drift.md`, 미수 3)
- 추출 1차 기준선 — `docs/11-obligation-extraction-baseline.md`. Sonnet 5, 골든셋 15건,
  **프롬프트 무조정**. EMPTY 2/3, IMPACT 근거 확보 8/10, **추출 단계 인용 폐기 0건**,
  실측 비용 $0.7675. 낮은 4건은 Opus 대조로 "모델 한계 2 / 프롬프트 한계 2"를 갈랐다.
  **개선 전에 이 수치를 기준선으로 남긴다.** 두 가지를 함께 박아 뒀다 —
  **폐기 0건은 "모델이 날조하지 않는다"가 아니라 "적대적 입력이 없었다"일 수 있고**
  (6단계 전까지 결론 금지), **더 강한 모델이 함정을 더 많이 물었다**(F-6 실측 근거,
  성능 문제를 모델 교체로 풀지 않는다)
- 위임 승격 — `retrieval/delegation.py`, `promote.py` (ADR-017). **문서 본문이 선언한
  위임 관계를 파싱해** 상위 정책 조항을 후보로 올린다. 재현율@k 0.7667 → 0.8167,
  **R-22 3항목 중 1항목 회수**. 나머지 2항목은 후보를 18조로 좁혀도 7·8위였고 점수가
  사실상 평평했다 — **정책 계층은 문단 유사도로 다룰 수 없다**는 더 강한 근거다
- 영향평가·gate 3단·evaluator-optimizer — `prompts/impact.py`, `verification/`,
  `pipeline/impact.py`. **부서 배정도 인용이다** — 근거 조항과 표현이 필수 키이고
  gate 2단이 그 인용문까지 대조한다. 간접 도출(근거 조항 ≠ 영향 문단)은 코드가 표시한다
- 승인 게이트 — `graph/`, `review/`, `api/app.py`, `db/migrations/011` (ADR-018).
  **세 겹이다** — 그래프 구조(`enqueue_actions` 의 유일한 진입점이 `human_review`),
  DB 권한(`app_graph` 는 승인 레코드를 쓸 수 없다), FK(`action_outbox` 는 승인 없이
  존재할 수 없다). 무너지는 방식이 다른 방어를 겹쳤고 `tests/security` 가 셋 다 검사한다
- 4단계 측정 — `docs/12-impact-assessment-results.md`. IMPACT 근거 확보 9/10(3단계 8/10),
  이관 4/15, 비용 $2.6333(3.43배). 두 가지가 예고대로 되지 않았다 —
  **기준선이 예고한 모순 필터는 전수 관측에서 기각됐고**(정답 출력에도 발화했다),
  **gate 3단이 case-013 을 잡지 못했다**. 검증기의 판정은 정확했는데 **재작성이 주장을
  고치지 않고 약화시켜** 통과시켰다
- **재작성 루프를 껐다** (`MAX_REVISIONS=0`, ADR-013 정정 이력). 근거는 전수 관측 —
  **재작성이 새 근거를 찾은 사례가 15건 중 0건**이다. 하는 일은 주장을 인용문에 맞춰
  낮추는 것이고, 그 뒤집힘은 정답(case-002)과 오답(case-013)을 가리지 않는다.
  대가는 IMPACT 적중 9/10 → 8/10 이며 그 대가를 치른다
- **de-anchored 검증기를 만들었고 채택하지 않았다** (`prompts/deanchored.py`,
  `GroundingMode`). 문헌이 지목한 기전(판정자가 후보를 보고 판정한다)은 실재하지만,
  그 처방이 우리 과제에서는 **재현율만 깎았다** — 정답 적중 9/10 → 6/10, 부서 재현율
  0.70 → 0.25, 비용 +39%, 그리고 case-013 은 여전히 못 잡았다.
  **기전이 맞다는 것과 처방이 듣는다는 것은 다른 문제다.** 코드는 대조용으로 남긴다

- 신뢰 등급과 인젝션 스캔 범위 — `guards/trust.py`, `db/migrations/012` (R-23 닫힘).
  **스캐너가 `str` 이 아니라 `UntrustedText` 를 받는다** — 사내 문서 텍스트로는 그 타입을
  만들 수 없으므로 trusted 텍스트가 스캐너에 도달하는 경로가 타입 수준에서 없다.
  감도는 한 글자도 바꾸지 않았다. 새던 곳은 지시서가 지목한 `pipeline/` 두 곳이 아니라
  **셋**이었고(gate 3단 검증기 입력이 호출 수로는 가장 많았다), 관측된 오탐도 하나가
  아니라 **넷**이었다 (`docs/13-injection-scan-scope.md`). **개정 조문 발화 0건은
  "스캐너가 유효하다"가 아니다** — 적대적 입력을 본 적이 없다는 뜻이며 6단계 전까지
  결론 금지다
- 킬 스위치 3종 실동작 — `guards/killswitch.py`, `adapters/switches.py`,
  `db/migrations/013` (ADR-019). **상태가 DB 에 있고 값 변경이 행으로 남는다** —
  환경변수는 "재배포 없이 60초 내 반영"도 "누가 언제 왜"도 만족하지 못한다.
  꺼지면 **예외로 멈춘다**(빈 결과가 아니다). 멈춘 이유를 셋으로 구별한다 —
  `OFF`/`NEVER_SET`/`UNAVAILABLE`. **어떤 서비스 role 도 스위치를 켤 수 없다**

- 변동성 실측 — `docs/14-variability-protocol.md`(기준을 **측정 전에** 고정),
  `docs/15-variability-results.md`. **경로에 따라 다르다** — 검색 3회 완전 일치(편차 0),
  LLM 은 집계값이 거의 안 흔들리는데(적중 8/8/8, 적중 케이스 집합까지 동일)
  **케이스 단위로 15건 중 10건이 흔들린다**(R-27). `hit` 은 안정적이고
  `impact_status`·`departments` 가 흔들린다 — **"무엇을 찾는가"는 안정적이고
  "어떻게 판정하고 누구에게 배정하는가"가 불안정하다**
- 단순 베이스라인 대조 — `docs/16-baseline-comparison.md`. **EASY 1.0000 은 우리 성과가
  아니다**(조 번호 문자열 매칭이 5건 전부 잡는다). **LLM 은 재현율을 낮추고**
  (0.8167 → 0.4583) **정밀도를 올린다**(0.1604 → 0.80, decoy 11 → 2). 기여는
  「찾기」가 아니라 **「고르기」와 「모른다고 말하기」**다

- 적대적 세트 **19건** — `evals/datasets/adversarial/`, `docs/18-adversarial-results.md`.
  **차단율 0.9412 (16/17), 실패 1건을 그대로 공개한다.** 뚫린 것은 가짜 대화 이력을
  **항 안쪽**에 심은 경우이며, 대조쌍으로 **위치 하나가 탐지와 차단을 동시에 가른다**를
  확인했다. 두 문제를 나눠 등재했다 — 스캐너 `^` 앵커는 R-28, **격리 구조가 뚫린 것은
  R-29**(원인 미확인, n=1 로 구조를 바꾸지 않는다).
  **인용 날조 시도는 8종 유도(평서문 4 + 정당화·압박·권위·예시·반복 4)에서 0건** —
  (a)에 유리하나 **확인했다고 쓰지 않는다.** 분모가 0 인 지표는 지표가 아니다.
  정상 15건 오탐 0건 유지. `scanner_fires` 와 `instruction_followed` 를 나눠 잰 덕에
  **뚫린 자리가 스캐너 사각과 겹친다**는 것이 보였다. 비용 $1.86

- 골든셋 **15건 → 42건** — `docs/20-golden-set-scale.md`, `docs/21-golden-42-results.md`.
  **어려운 쪽으로 늘렸다**(`docs/16` §5 의 지시대로 EASY 를 늘리지 않았다). 그 결과
  **병목이 케이스 구성에 따라 뒤집힌다** — 기존 15건은 「선택」이 병목((b) 0.42),
  신규 27건은 「검색」이 병목((a) 0.71). 어느 쪽이 운영에 가까운지를 **인용 밀도 대조**로
  갈랐고(개정 조문 0.3750 vs 사내 0.1447, 판정 폭의 2.63배) 재순위 미도입 근거를 유지했다.
  IMPACT 근거 확보 14/20, 영향 없음 20/22, decoy 인용 1, 비용 $3.9736

- **지표 종합** — `docs/23-metrics-summary.md`. **기획서 12.2 목표 6개 중 달성 1개이고
  그 1개(인용 정확도 1.0000)는 구조가 보장하는 항등식이라 성적이 아니다.**
  세 가지를 새로 세웠다 — (1) **인용 적합도 사람 판정 20건**(gate 3단 `SUPPORTED` 전수).
  판정지에서 **기계 판정 5종을 전부 뺐고**(등급·사유·케이스 ID·유형·실행 판정) 시드로
  섞었다. 결과는 **F-6 = 0.0000, 95% 상한 0.168** — **「없다」가 아니라 「20건에서
  관측되지 않았다」**이며, 무엇보다 **알려진 F-6 사례(case-013)가 이 척도를 통과했다**
  (「인용이 주장을 받치는가」는 묻고 **「그 인용을 했어야 하는가」는 묻지 않는다**).
  판정 기준이 **하한만 고정돼 있었다**는 것도 판정 뒤에 드러났다 — 다르게 읽으면
  불일치율이 0.05 → 0.40 이다,
  (2) **검정력 분석**(Miller 2024) — **KURE 채택·de-anchored 기각·`MAX_REVISIONS=0`
  셋 다 통계적으로 유의하지 않다.** 뒤집지 않되 인용할 때 그 사실을 붙인다.
  δ=0.05 를 잡으려면 IMPACT 케이스 **79건**이 필요하고 지금은 20건이다,
  (3) **CI 게이트는 LLM 지표에 붙이지 않는다** — 편차보다 넓고 MDE 보다 좁은 임계치가
  존재하지 않는다. 검색 지표에는 붙일 수 있다(결정론·$0). 조건은 §4.3.
  **「인용 폐기 0건」을 정정했다** — 추출 단계의 사실이며 **영향평가 단계에서는 4단계 1건 ·
  42건 4건이 발화했고 전부 `QUOTE_NOT_FOUND`** 다. 6단계 지출은 **$14.04**(`llm_invocation`
  질의)이며 **문서 합산보다 $0.87 크다** — 무효·중단 실행의 호출은 결과 파일에 남지 않는다

- **라우팅을 검토하고 접었다** — `docs/22-routing-precheck.md`, ADR-013 「재검토 이력」.
  골든 42건(`docs/21`)에서 **입력이 7종으로 갈려 배제 근거("입력이 한 종류다")가 무효가
  됐고**, 그래서 다시 쟀다. **재보니 다른 근거로 같은 결론이었다** — 경량 경로 C 는
  발동률 0%(`search()` 에 유사도 임계가 없어 검색이 비지 않는다), 점수 임계는 근거 없음
  (RRF 는 순위만 본다 · 원점수는 겹치고 방향이 라우팅 축과 다르다), **절감 상한 13.26%**.
  **「타법개정이 LLM 3회를 부른다」가 틀렸다 — 2회다**(인용 0건이라 gate 3단이 안 돈다).
  13%를 아끼려고 F-1 경로를 만들지 않는다. **de-anchored 와 같은 형태이되 다른 점이
  하나 있다 — 그때는 $1 을 쓰고 알았고 이번은 $0 으로 알았다**(LLM 미호출)

**검증 없이 쌓지 않는다.** 위 항목을 채우는 것은 1단계 이후의 일이며, 각각 원문 확인 또는
평가 데이터셋과 함께 들어온다.
