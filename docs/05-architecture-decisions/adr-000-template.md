# ADR-000: (결정을 한 문장으로)

<!-- 파일명 규칙: adr-00N-kebab-case-title.md
     번호는 재사용하지 않는다. 폐기된 ADR도 번호를 유지한 채 남긴다.
     번호가 비면 "그 결정이 있었는지"를 아무도 확인할 수 없다. -->

| | |
|---|---|
| **상태** | 제안됨 / 채택됨 / 폐기됨 / 대체됨(ADR-00N) |
| **일자** | YYYY-MM-DD |
| **결정자** | |
| **관련 원칙** | 원칙 N (CLAUDE.md §1) |
| **관련 ADR** | |

---

## 맥락

<!-- 무엇을 쓸 것인가:
     - 어떤 상황에서 이 결정이 필요해졌는가
     - 제약 조건 (기술적/규제적/조직적)
     - 이 결정을 미룰 수 없는 이유
     아직 결정하지 않아도 되는 것을 결정하는 것도 비용이다. -->

---

## 결정

<!-- 한 문단으로 무엇을 하기로 했는지 쓴다. 이유는 아래에 쓴다. -->

---

## 목적

<!-- 이 결정이 무엇을 달성하려 하는가.
     4항목 구조의 첫 번째 항목 (CLAUDE.md §3). -->

---

## 구현 이유

<!-- 왜 이 방식인가. 검토한 대안을 각각 적고, 각 대안을 택하지 않은 이유를 쓴다.
     "대안을 검토했다"가 아니라 대안의 이름과 탈락 사유를 적는다.
     탈락 사유가 "익숙하지 않아서"라면 그렇게 적는다. 사후에 미화하지 않는다.

     | 대안 | 장점 | 택하지 않은 이유 |
     |---|---|---|
-->

---

## 트레이드오프

<!-- 무엇을 포기했는가. 이 절이 ADR의 핵심이다.

     포기한 것이 없다고 생각되면 "없음"이라 쓰지 말고, 왜 없는지를 쓴다.
     대개는 아직 대가를 발견하지 못한 것이다.

     함께 쓸 것:
     - 이 트레이드오프가 잘못된 선택이었음을 알게 되는 신호는 무엇인가
     - 되돌리는 비용은 얼마나 되는가 (되돌릴 수 없다면 그렇게 명시한다) -->

---

## 엣지 케이스

<!-- 어떤 예외 상황을 상정했고 각각을 어떻게 처리하는가.
     - 실패했을 때의 동작 (fail-closed 인가 fail-open 인가, 그리고 왜)
     - 부분 실패, 동시성, 재시도
     - 이 결정이 다른 원칙과 충돌하는 지점과 그 해소 방법 -->

---

## 결과

<!-- 채택 후 실제로 무엇이 바뀌었는가. 채택 시점에는 비워 두고 나중에 채운다.
     이 절이 계속 비어 있는 ADR이 많다면, ADR을 쓰는 행위 자체가 형식화된 것이다. -->
