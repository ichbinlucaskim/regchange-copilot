# 법제처 Open API 실측 명세

> **작성 원칙**: 이 문서에는 **실제 호출로 확인한 것만** 적는다. 모든 서술에 근거 픽스처
> 파일명을 붙인다. 확인하지 못한 것은 "미확인"으로 표시하고, 무엇을 해야 확인되는지 적는다.
> 추측을 사실처럼 쓰지 않는다 (CLAUDE.md §5.1).

- **탐색 일자**: 2026-08-10
- **픽스처 위치**: `tests/fixtures/law_api/` (목록은 그곳의 `README.md`)
- **탐색 스크립트**: `scripts/exploration/fetch.py`, `scripts/exploration/analyze_articles.py`
- **함정 목록**: `docs/api-exploration/edge-cases.md`

---

## 1. 엔드포인트와 공통 파라미터

### 1.1 Base URL

```
https://www.law.go.kr/DRF/lawSearch.do    # 목록 조회
https://www.law.go.kr/DRF/lawService.do   # 본문 조회
```

`https://law.go.kr/DRF/...`(www 없음)도 동일하게 동작한다. `https://law.go.kr/OpenApiService/...`
는 **404**다 — 이 프로젝트의 `.env`에 처음 설정되어 있던 값이며 0.7단계에서 정정했다.

### 1.2 공통 파라미터

| 파라미터 | 필수 | 값 | 근거 |
|---|---|---|---|
| `OC` | 필수 | 발급받은 사용자 식별값 | 전 픽스처 |
| `target` | 필수 | 아래 target 표 참조 | 전 픽스처 |
| `type` | 필수 | `XML` / `JSON` / `HTML` | XML만 실측(`law_009244_mst252787.xml` 등). JSON은 **미확인** |
| `display` | 선택 | 목록 조회 반환 건수 | `search_law_teukgeum.xml` |
| `page` | 선택 | 목록 페이지 (1부터) | 실측 확인, §6.1 |

**`OC`는 응답 본문에 그대로 되돌아온다.** `법령상세링크`, `조문링크`,
`조문변경이력상세링크` 필드가 요청 URL을 통째로 담는다 (`search_law_teukgeum.xml`,
`dayjochg_regdt20250401.xml`). 응답을 로그·픽스처·캐시에 남길 때 반드시 마스킹해야 한다.

---

## 2. target 코드

| 용도 | 엔드포인트 | target | 상태 |
|---|---|---|---|
| 법령 목록 검색 | lawSearch.do | `law` | ✅ 동작 |
| 법령 본문 | lawService.do | `law` | ✅ 동작 |
| 시행일 법령 목록 | lawSearch.do | `eflaw` | ✅ 동작 |
| 시행일 법령 본문 | lawService.do | `eflaw` | ✅ 동작 |
| **법령 변경이력 목록** | lawSearch.do | `lsHstInf` | ✅ 동작 (`regDt` 필수) |
| **일자별 조문 개정 이력 목록** | lawSearch.do | `lsJoHstInf` | ✅ 동작 |
| **조문별 변경 이력 목록** | lawService.do | `lsJoHstInf` | ✅ 동작 (`ID`+`JO` 필수) |
| 법령 연혁 목록 | lawSearch.do | `lsHistory` | ⛔ **미신청 — 이 OC로 접근 불가** |
| 행정규칙 목록 | lawSearch.do | `admrul` | ✅ 동작 |
| 행정규칙 본문 | lawService.do | `admrul` | ✅ 동작 |
| 신구법 비교 목록 | lawSearch.do | `oldAndNew` | ✅ 응답 확인(구조 **미확인**) |
| 3단 비교 목록 | lawSearch.do | `thdCmp` | ✅ 응답 확인(구조 **미확인**) |

**일자별과 조문별 조문 변경이력은 같은 target(`lsJoHstInf`)을 쓰고 엔드포인트로만
갈린다.** `lawSearch.do`면 일자별, `lawService.do`면 조문별이다. 응답 스키마도 다르다
(§5.2, §5.3).

`lsHistory`는 활용가이드에 존재하지만 이 OC로는 **미신청 상태**다. 호출하면 HTTP 200에
"미신청된 목록/본문에 대한 접근입니다" HTML 안내 페이지가 온다
(`error_target_lsHistory.html`). 필요해지면 open.law.go.kr에서 해당 API를 신청해야 한다.
현재로선 **법령 연혁 목록 없이도** 조문별 변경 이력(`lsJoHstInf`)이 각 버전의
법령일련번호를 주므로 대체 가능하다 (`jochg_009244_jo000200_full.xml`).

---

## 3. target별 파라미터

### 3.1 `law` 본문 (lawService.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `MST` | ID와 택1 | 법령일련번호(법령 버전 식별자) | `law_009244_mst252787.xml` |
| `ID` | MST와 택1 | 법령ID(법령 자체 식별자) | **미확인** — 본문 조회에서는 MST만 실측 |
| `JO` | 선택 | 조문 6자리. 생략 시 전체 조문 | 활용가이드. 본문 조회에서는 **미확인** |

**`MST`와 `ID`는 다른 것이다.** 특금법의 `법령ID`는 `009244`로 고정이고,
`법령일련번호(MST)`는 버전마다 다르다 — 제정판 59509부터 현행 252787까지 28개
(`jochg_009244_jo000200_full.xml`).

### 3.2 `eflaw` 본문 (lawService.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `MST` | ID와 택1 | 법령일련번호 | `eflaw_009256_mst261379_ef20240326.xml` |
| `efYd` | 필수 | 시행일자 8자리 | 위와 동일. 같은 MST에 efYd 3개로 각각 다른 응답 |
| `JO` | 선택 | **조문번호 4자리 + 가지번호 2자리 = 6자리** | 활용가이드에 명시 |
| `chrClsCd` | 선택 | 한글(010202)/원문(010201) | 활용가이드. **미확인** |

`JO`가 6자리 = 조문번호(4) + 가지번호(2)라는 것은 활용가이드에 명시되어 있고,
`조문이동이전`/`조문이동이후` 및 변경이력 API의 `조문번호` 필드가 모두 같은 6자리
형식을 쓴다 (`jochg_009244_jo000502.xml`의 `<조문번호>000502</조문번호>` = 제5조의2).

### 3.3 `lsHstInf` — 법령 변경이력 목록 (lawSearch.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `regDt` | **필수** | 법령 변경일 8자리 | `lschg_regdt20240719.xml` |
| `org` | 선택 | 소관부처 코드 | 활용가이드. **미확인** |
| `display`, `page` | 선택 | 페이지네이션 | 실측 |

**법령ID나 MST로는 조회할 수 없다.** 날짜 기준 API다. `MST`/`ID`/`LID`/`lsId`를 넘기면
`totalCnt=0`만 돌아온다 — 파라미터 오류인데 0건처럼 보인다.

### 3.4 `lsJoHstInf` — 일자별 조문 개정 이력 (lawSearch.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `regDt` | 선택 | 조문 개정일 8자리 | `dayjochg_regdt20250401.xml` |
| `fromRegDt` / `toRegDt` | 선택 | 조회 기간 | 활용가이드. **미확인** |
| `ID` | 선택 | 법령ID | 단독으로 주면 0건 (`error_dayjochg_id_only_zero.xml`) |
| `JO` | 선택 | 조문 6자리 | **미확인** |
| `org` | 선택 | 소관부처 코드 | **미확인** |

실측상 **`regDt`를 주어야 결과가 나온다.** `ID`만 주면 `totalCnt=0`이다.

### 3.5 `lsJoHstInf` — 조문별 변경 이력 (lawService.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `ID` | **필수** | **법령ID** (MST 아님) | `jochg_009244_jo000200_full.xml` (ID=009244) |
| `JO` | **필수** | 조문 6자리 | 위와 동일 (JO=000200 → 제2조) |
| `display` | 선택 | 기본 20 | `jochg_009244_jo000200.xml`은 28건 중 20건만 반환 |

### 3.6 `admrul` (lawSearch.do / lawService.do)

| 파라미터 | 필수 | 의미 | 근거 |
|---|---|---|---|
| `query` | 선택 | 행정규칙명 검색 | `search_admrul_aml.xml` |
| `org` | 선택 | 소관부처 코드. `1160100`으로 금융정보분석원 필터 성공 | 실측 |
| `ID` (본문) | 필수 | **행정규칙일련번호**를 넣는다 (예: `2100000267264`) | `admrul_2100000267264.xml` |

**`ID` 파라미터에 `행정규칙ID`를 넣으면 실패한다.** 검색 결과의 `행정규칙ID`는 `2037208`,
`행정규칙일련번호`는 `2100000267264`인데, 본문 조회의 `ID`는 후자를 받는다.
검색 응답의 `행정규칙상세링크` 필드가 정답(`target=admrul&ID=2100000267264`)을 담고 있으므로
그 값을 그대로 쓰는 것이 안전하다. 잘못 넣으면 "일치하는 행정규칙이 없습니다.
행정규칙명을 확인하여 주십시오"라는 **오도하는** 메시지가 온다
(`error_admrul_wrong_id_param.xml`).

`org=1160100`(금융정보분석원/금융위원회)로 필터하면 우체국금융 규정이 제거되어 3건 → 2건이
된다 (실측).

---

## 4. 법령 본문 응답 스키마 (target=law)

근거: `law_009244_mst252787.xml`(특금법), `law_010513_mst283193.xml`(자본시장법),
`law_001565_mst280405.xml`(소득세법), `law_010199_mst280277.xml`(전자금융거래법)

```
/법령                         @법령키
  /기본정보                    법령ID, 공포일자, 공포번호, 언어, 법종구분@법종구분코드,
                              법령명_한글, 법령명_한자, 법령명약칭, 제명변경여부,
                              한글법령여부, 편장절관, 소관부처@소관부처코드, 전화번호,
                              시행일자, 제개정구분, 별표편집여부, 공포법령여부,
                              연락부서/부서단위@부서키(소관부처명, 소관부처코드, 부서명, 부서연락처),
                              공동부령정보
  /조문
    /조문단위                  @조문키   ← 태그가 아니라 XML 속성이다
      조문번호, 조문여부, 조문시행일자, 조문이동이전, 조문이동이후,
      조문변경여부, 조문내용                     (항상 존재)
      조문제목, 조문참고자료                      (조건부)
      조문가지번호                               (가지 조문에만)
      조문제개정유형, 조문제개정일자문자열          (드묾 — 특금법 34개 중 1개)
      /항
        항번호, 항내용                            (조건부 — 아래 §4.3)
        /호  호번호, 호내용
        /목  목번호, 목내용                        ← 호가 아니라 항의 자식이다
  /부칙
    /부칙단위 @부칙키           부칙공포일자, 부칙공포번호, 부칙내용
  /별표                        (법령에 따라 없음 — §4.5)
  /개정문/개정문내용
  /제개정이유/제개정이유내용
```

### 4.1 필드별 의미와 관측된 값 분포

| 필드 | 관측 값 | 근거 |
|---|---|---|
| `조문키` (속성) | 7자리 숫자 | 4개 법령 1,189개 조문단위 전부 7자리 |
| `조문번호` | 1 ~ 449 | 최대값은 자본시장법(`law_010513_mst283193.xml`) |
| `조문가지번호` | 2 ~ 27 | 최대값은 소득세법(`law_001565_mst280405.xml`) |
| `조문여부` | **`전문` / `조문`** (Y/N 아님) | 전 법령 픽스처. `전문`=편장절관 제목행 |
| `조문변경여부` | `Y` / `N` | 특금법 1/34, 소득세법 27/393, 전자금융거래법 15/80, 자본시장법 3/682 |
| `조문이동이전`·`조문이동이후` | `''`(빈 문자열) 또는 `000000` | §4.4 — **실질 정보 없음** |
| `조문시행일자` | 8자리. **문서 `시행일자`와 항상 동일** | §4.6 |
| `항번호` | `①`~`⑪` 원문자. CDATA 아님 | `law_009244_mst252787.xml` |
| `호번호` | `1.`, `2.`, … | 위와 동일 |
| `목번호` | `가.`~`하.` 다음 **`거.`, `너.`** | `law_009244_mst252787.xml` 제2조 제1호 |
| `조문참고자료` | `[전문개정 2011.5.19]` 형태 | 특금법 27건 |

### 4.2 조문키 체계 — **4개 법령 1,189개 조문에서 검증됨**

```
조문키 = [조문번호 4자리][가지번호 2자리][유형 1자리]
유형: 0 = 편장절관 제목행(조문여부="전문")
      1 = 조문 본체(조문여부="조문")
```

`scripts/exploration/analyze_articles.py`가 `조문번호`·`조문가지번호`·유형자리로 키를
재구성해 원본과 대조한다. **불일치 0건**:

| 법령 | 조문단위 | 최대 조문번호 | 최대 가지번호 | 재구성 불일치 |
|---|---|---|---|---|
| 특금법 | 34 | 20 | 4 | 0 |
| 전자금융거래법 | 80 | 51 | 6 | 0 |
| 소득세법 | 393 | 177 | 27 | 0 |
| 자본시장법 | 682 | 449 | 23 | 0 |

- 조문번호가 4자리를 넘는 사례: **관측되지 않음**(최대 449). 4자리 한계에 도달하려면
  조문 1만 개짜리 법령이 필요하다.
- 가지번호가 2자리를 넘는 사례: **관측되지 않음**(최대 27).
- 유형 자리에 `0`/`1` 외의 값: **관측되지 않음.**

**단, 조문키는 유일 식별자가 아니다** — §4.7 참조.

### 4.3 항/호/목의 실제 구조

관측된 규칙 (`law_009244_mst252787.xml`, 4개 법령 전부 일관):

1. **항이 없고 호도 없는 조문**: `<항>` 태그 자체가 없고, `조문내용`에 조문 전문이 들어간다.
   예: 제1조(목적), 제8조, 제18조, 제19조. 4개 법령 합계 245건.
2. **항 번호가 붙는 조문**: `<항>`이 여러 개, 각각 `항번호`(`①`…)와 `항내용`을 갖는다.
   이때 `조문내용`은 **제목만** 담는다 — 예: `제4조의2(금융회사등의 고액 현금거래 보고)`.
3. **항 구분 없이 호만 있는 조문**: **`항번호`가 없고 `항내용`이 빈 문자열인 껍데기
   `<항>`이 하나 삽입**되고 그 아래 `<호>`가 달린다. 특금법 제2조·제16조가 이 경우.
   4개 법령 합계 51건.
4. **목은 `<호>`의 자식이 아니라 `<항>`의 자식이다.** 문서 순서상 직전 `<호>`에 귀속되며,
   XML에 귀속을 나타내는 필드는 **없다**.
   - 4개 법령 전부에서 "선행 `<호>` 없이 등장한 `<목>`" **0건** — 문서 순서로 귀속 판별이
     성립한다는 근거다. 순서를 보존하지 않는 파서(dict 변환 등)를 쓰면 귀속이 소실된다.

### 4.4 `조문이동이전` / `조문이동이후` — **실제 이동을 반영하지 않는다**

- 값은 두 가지뿐이다: 빈 문자열, 또는 `000000`.
- **`조문변경여부`와 완전히 연동한다**: `Y` ⇔ `000000`, `N` ⇔ 빈 문자열. 4개 법령
  1,189개 조문 전부 이 규칙을 벗어나지 않는다.
- 조사 범위(본문/연혁/eflaw 픽스처 13개 문서 · 고유 법령ID 8개 · 합계 **1,957개 조문단위**)에서 **`000000`이 아닌
  값 0건**.
- 그런데 조문 이동은 **실제로 일어난다**: 특금법 2011판과 2020판을 대조하면 12개 조문이
  이동했다 (`law_009244_mst113262_v20110519.xml` ↔ `law_009244_mst215971_v20200324.xml`).

  | 이동 전 | 이동 후 | 조문제목 |
  |---|---|---|
  | 0006001 | 0009001 | 외국환거래자료 등의 통보 |
  | 0007001 | 0010001 | 수사기관 등에 대한 정보 제공 |
  | 0008001 | 0011001 | 외국금융정보분석기구와의 정보 교환 등 |
  | 0009001 | 0012001 | 금융거래정보의 비밀보장 등 |
  | 0010001 | 0013001 | 자료 제공의 요청 등 |
  | **0012001** | **0014001** | 다른 법률과의 관계 ← 순서 역전 |
  | **0011001** | **0015001** | 금융회사등의 감독ㆍ검사 등 ← 순서 역전 |
  | 0013001 · 0014001 | 0016001 · 0017001 | 벌칙 (**2×2 모호** — 양쪽 모두 같은 제목 2건) |
  | 0015001 | 0018001 | 징역과 벌금의 병과 |
  | 0016001 | 0019001 | 양벌규정 |
  | 0017001 | 0020001 | 과태료 |

  고유 출발 조문키 12개 → 고유 도착 12개. 제목 기준 매칭은 벌칙 때문에 **14행**으로
  전개되며 4행이 모호하다.

  **두 버전 모두 이동 필드는 `000000`이다.** 즉 이 필드로는 이동을 알 수 없다.

### 4.5 별표·서식·이미지

- `기본정보/별표편집여부`는 항상 존재한다.
- `/법령/별표` 트리는 **법령에 따라 있고 없다**: 자본시장법에는 있고 특금법(target=law)에는
  없다.
- **같은 법령이라도 target에 따라 다르다**: 특금법은 `target=law`(`law_009244_mst252787.xml`)에
  별표 트리가 없지만 `target=eflaw`(`eflaw_009256_mst261379_ef20240326.xml`)에는 있다.
- 별표 하위 필드: `별표단위`, `별표번호`, `별표가지번호`, `별표구분`, `별표제목`,
  `별표내용`, `별표시행일자`(eflaw만), `별표이미지파일명`, `별표HWP파일명`,
  `별표PDF파일명`, `별표서식파일링크`, `별표서식PDF파일링크`, `별표서식이미지파일링크`.
- **별표 본문의 실질은 첨부파일(HWP/PDF/이미지)일 수 있다.** 텍스트 diff가 성립하지 않는
  범위가 존재한다.

### 4.6 `조문시행일자` — 조문별로 갈리지 않는다

- 본문 API(`law`, `eflaw`)의 `조문시행일자`는 **모든 조문이 문서 `시행일자`와 동일**하다.
  본문/eflaw 픽스처 13개 전부에서 종류가 1개다.
- `eflaw`도 마찬가지다: MST 261379를 efYd 20240326 / 20240627 / 20240719로 조회하면 46개
  조문이 **각각 그 efYd 하나로 평탄화**되어 나온다.
- **조문별 시행일이 실제로 갈리는 것은 일자별 조문 개정 이력 API에서만 관측된다**
  (`dayjochg_regdt20250401.xml`, §5.2).

### 4.7 조문키는 유일 식별자가 아니다

`조문여부=전문`(편장절관 제목행)은 **뒤따르는 조문의 키를 공유**한다. 한 조문 앞에 여러
계층 제목이 붙으면 같은 키가 중복된다.

- 자본시장법: `0011000`이 **3회** — "제2편 금융투자업", "제1장 금융투자업의 인가 및 등록",
  "제1절 인가요건 및 절차" (`law_010513_mst283193.xml`)
- 소득세법: `0012000`이 2회, `0014000`이 2회 (`law_001565_mst280405.xml`)

또한 **편/장/절/관의 계층은 XML 구조로 표현되지 않는다.** 모든 제목행이 `조문단위`로
평면 나열되고, 계층은 `조문내용` 텍스트(`제2편 …`, `제1장 …`, `제1절 …`, `제1관 …`)를
파싱해야만 알 수 있다.

---

## 5. 변경이력 응답 스키마

### 5.1 `lsHstInf` — 법령 변경이력 목록

근거: `lschg_regdt20240719.xml` (regDt=20240719 → totalCnt 71)

```
/LawSearch  target, totalCnt
  /law      법령일련번호, 현행연혁코드, 법령명한글, 법령ID, 공포일자, 공포번호,
            제개정구분명, 소관부처코드, 소관부처명, 법령구분명, 시행일자,
            자법타법여부, 법령상세링크
```

**조문 수준 정보가 없다.** "그날 무엇이 바뀐 법령인가"만 알려준다.

### 5.2 `lsJoHstInf` (lawSearch.do) — 일자별 조문 개정 이력

근거: `dayjochg_regdt20250401.xml` (regDt=20250401 → 83개 법령)

```
/LawSearch  target, totalCnt
  /law
    /법령정보   법령일련번호, 법령명한글, 법령ID, 공포일자, 공포번호,
                제개정구분명, 소관부처코드, 소관부처명, 법령구분명, 시행일자
    /조문정보
      /jo @num  조문번호(6자리), 변경사유, 조문링크,
                조문변경이력상세링크, 조문개정일, 조문시행일
```

**이 API가 조문 수준 시행일을 주는 유일한 곳이다.** 83개 법령 중 **17개**에서 문서
`시행일자`와 조문 `조문시행일`이 갈린다:

| 법령 | 문서 시행일자 | 관측된 조문시행일 |
|---|---|---|
| 모자보건법 | 20270101 | 20250401, 20270101 |
| 아동복지법 | 20251002 | 20250401, 20251002, 20260402 |
| 지방공기업법 | 20260101 | 20250401, 20251002, 20260101 |
| 응급의료에 관한 법률 | 20260402 | 20260402, 20270402 |

### 5.3 `lsJoHstInf` (lawService.do) — 조문별 변경 이력

근거: `jochg_009244_jo000200_full.xml` (ID=009244, JO=000200 → 28건)

```
/LawService  target, totalCnt, 법령ID, 법령명한글
  /law
    /법령정보   법령일련번호, 공포일자, 공포번호, 제개정구분명,
                소관부처코드, 소관부처명, 법령구분명, 시행일자
    /조문정보   조문번호(6자리), 변경사유, 조문링크, 조문변경일
```

`조문정보`가 **단일**이다 (일자별 API는 `jo` 반복). 같은 target인데 스키마가 다르다.

### 5.4 `변경사유` 값 — 관측된 어휘는 6종

7개 날짜(`dayjochg_regdt*.xml`) + 특금법 조문별 이력 3건(`jochg_*.xml`), **픽스처로
전수 집계 가능한 4,853건**:

| 값 | 건수 |
|---|---|
| `조문변경` | 3,408 |
| `일괄개정` | 533 |
| `제정` | 484 |
| `본조신설` | 375 |
| `전부개정` | 34 |
| `폐지제정` | 19 |

조사한 날짜: 20200324, 20210105, 20230103, 20240109, 20250114, 20250401, 20260113.

> **정정 이력**: 0.7단계 최초 집계에서는 `display=100`으로 조회해 어휘가 4종
> (`조문변경`/`본조신설`/`제정`/`일괄개정`)으로 보였다. `display=1000`으로 다시 받으니
> `전부개정`·`폐지제정`이 추가로 나타났다 — §7.1의 잘림 함정에 집계 자체가 걸린 사례다.
> 현재 수치는 저장된 픽스처에서 재현 가능하다.

**이동을 나타내는 값은 여전히 관측되지 않았다.** `전부개정`과 `폐지제정`은 법령 전체의
개정 방식이지 조문의 이동을 가리키지 않는다 — `전부개정` 34건은
`dayjochg_regdt20210105.xml` 한 파일에 몰려 있고, 같은 법령의 조문들이 일괄로 그 값을
갖는다.

### 5.5 `조문변경여부`(본문) vs 변경이력 API — 무엇이 다른가

| | 본문의 `조문변경여부` | 변경이력 API |
|---|---|---|
| 알려주는 것 | **이번 버전에서 바뀐 조문인가** (Y/N) | 조문이 **역대 언제** 바뀌었는가 |
| 시간 범위 | 해당 MST 한 버전 | 제정부터 현재까지 전체 |
| 사유 구분 | 없음 | `제정`/`본조신설`/`조문변경`/`일괄개정` |
| 시행일 | 문서 시행일로 평탄화 | **조문별 시행일 제공**(일자별 API) |
| 변경 내용 | 없음 (바뀌었다는 사실만) | 없음 (바뀌었다는 사실만) |

**결론: 본문의 플래그만으로는 부족하다.** 이유 세 가지 —
(1) 조문별 시행일 분기는 일자별 API에만 있다,
(2) `본조신설`과 `조문변경`의 구분이 본문에는 없다,
(3) 본문 플래그는 한 버전에 대한 것이라 "언제부터 이 조문이 존재했는가"를 답하지 못한다.

**단, 둘 다 "무엇이 어떻게 바뀌었는가"는 주지 않는다.** 실제 텍스트 차분은 두 버전의 본문을
직접 비교해야 한다 — 원칙 1(결정론적 diff)이 필요한 이유가 여기서 실측으로 확인됐다.

---

## 6. 행정규칙 응답 스키마 (target=admrul)

근거: `admrul_2100000267264.xml` (자금세탁방지 및 공중협박자금조달금지에 관한 업무규정)

```
/AdmRulService                ← 루트 태그가 법령(`법령`)과 다르다
  /행정규칙기본정보  행정규칙일련번호, 행정규칙명, 행정규칙종류, 행정규칙종류코드,
                    발령일자, 발령번호, 제개정구분명, 제개정구분코드, 조문형식여부,
                    행정규칙ID, 소관부처명, 소관부처코드, 상위부처명,
                    담당부서기관코드, 담당부서기관명, 담당자명, 전화번호,
                    현행여부, 시행일자, 생성일자
  /조문내용         ← 202개가 평면으로 나열된다. 하위 구조 없음
  /부칙             부칙공포일자, 부칙공포번호, 부칙내용
  /제개정이유/제개정이유내용
  /첨부파일         첨부파일명, 첨부파일링크
```

**법령과 조문 구조가 같지 않다. 전혀 다르다.**

| | 법령(`law`) | 행정규칙(`admrul`) |
|---|---|---|
| 루트 | `법령` | `AdmRulService` |
| 조문 컨테이너 | `조문/조문단위` | `조문내용` (평면 반복) |
| 조문키 | `@조문키` 7자리 | **없음** (XML 속성 0개) |
| 조문번호 | `조문번호` 필드 | **없음** |
| 항/호/목 | 태그로 분리 | **없음** — 텍스트 안에 `\n  ②`로 들어있다 |
| 조문시행일자 | 있음 | **없음** |
| 편장절관 | 별도 `조문단위`(조문여부=전문) | 같은 `조문내용` 태그를 공유 |

`조문형식여부 = 'Y'`인데도 조문 구조가 제공되지 않는다. 실제 값 예시:

```
[0]  '제1장 총칙\n'                                   ← 장 제목
[1]  '제1조(목적) 이 규정은 「특정금융거래정보의 …'      ← 조문
[4]  '제1편 금융회사등(카지노사업자 제외)\n'            ← 편 제목
[7]  '제4조(이사회의 역할 및 책임) ① 금융회사등은 … \n  ② …'  ← 항이 텍스트 안에
[61] '제45조 <삭 제>\n'                                ← 삭제 조문
```

**이 문서가 사내 규정 매핑의 핵심 대상이므로, 조문 단위 처리를 하려면 별도 파서가 필요하다.**
정본이 첨부파일(hwpx/pdf)일 가능성도 있다 — 이 응답의 `첨부파일` 2건이 그 예다.

### 6.1 식별자 부재 — 재확인 결과

"조문번호만 없는 것"과 "식별자가 아예 없는 것"은 난이도가 다르므로 원문 바이트로 전수
재확인했다. 결과는 **후자**다.

| 검사 | 결과 |
|---|---|
| `<조문내용 …>` 여는 태그 고유 형태 | `<조문내용>` **한 가지뿐** (202회 전부 속성 없음) |
| 문서 전체 XML 속성 개수 | **0개** (법령은 `조문단위@조문키`, `법령@법령키` 등 보유) |
| `조문번호` 태그 | **없음** |
| `조문키` 태그·속성 | **없음** |
| 조문 관련 식별자 | 문서 수준의 `행정규칙일련번호`, `행정규칙ID`뿐 |

즉 **조문 식별자를 API가 전혀 주지 않으며, 텍스트에서 복원하는 수밖에 없다.**

텍스트 패턴 커버리지는 전수 분류가 가능하다 (202개 전부):

| 패턴 | 건수 | 예시 |
|---|---|---|
| `^제(\d+)조(?:의(\d+))?\s*\(` — 제목 있는 조문 | 151 | `제5조의2(준법감시인등의 역할 및 책임) ① …` |
| `^제\d+[편장절관]\s` — 편/장/절/관 제목 | 48 | `제1장 총칙`, `제1편 금융회사등(카지노사업자 제외)` |
| 제목 없는 삭제 조문 | 3 | `제45조 <삭 제>`, `제49조 <삭 제>`, `제131조 <삭 제>` |

삭제 조문은 괄호 제목이 없어 첫 패턴에 걸리지 않는다. 별도 패턴이 필요하다.

### 6.2 행정규칙의 마커 어휘는 법령과 다르다

`admrul_2100000267264.xml`에서 관측된 본문 내 마커는 **`<삭 제>` 9건뿐**이다.
법령 본문에 흔한 `<개정 …>`·`<신설 …>`은 **0건**.

- `<삭 제>`는 **`삭`과 `제` 사이에 공백**이 있다. `<삭제>`로 매칭하면 놓친다.
- 조문 전체 삭제(`제45조 <삭 제>`)와 항 단위 삭제(`제24조(…) ① <삭 제>`)가 모두 있다.

---

## 7. 운영 특성

### 7.1 페이지네이션

| 항목 | 활용가이드 | 실측 | 근거 |
|---|---|---|---|
| `display` 최대 | 100 | **상한 없음** — 500, 1000 모두 그대로 반환. 5000을 주면 totalCnt(4516)까지 반환 | 실측 |
| `page` | 1부터 | 정상 동작. 마지막 페이지는 부분 반환(page=46 → 16건) | 실측 |
| `totalCnt` | 전체 건수 | `numOfRows`는 요청한 display를 그대로 되돌려준다 | 실측 |

**가이드와 실제가 다르다. 실제를 신뢰하되 100 초과에 의존하지 않는 것이 안전하다** —
문서화되지 않은 동작은 예고 없이 바뀐다.

`display` 기본값 20 때문에 조용히 잘린다: `jochg_009244_jo000200.xml`은 `totalCnt=28`인데
20건만 담고 있다. 응답에 "잘렸다"는 표시가 없다.

### 7.2 에러 응답 — **HTTP 상태코드로 구분되지 않는다**

관측된 모든 응답이 **HTTP 200**이다. 실패는 본문 형태로만 구별된다.

| 상황 | 본문 | resultCode | 근거 픽스처 |
|---|---|---|---|
| 정상 (목록) | `<LawSearch>` + 키워드/section/page/numOfRows | `00` / `success` | `search_law_teukgeum.xml` |
| 정상 0건 (목록) | 위와 동일, `totalCnt=0` | `00` / `success` | `error_search_zero_result.xml` |
| 잘못된 MST (본문) | `<Law>일치하는 법령이 없습니다. …</Law>` | **없음** | `error_law_bad_mst.xml` |
| 잘못된 ID (행정규칙) | `<Law>일치하는 행정규칙이 없습니다. …</Law>` | **없음** | `error_admrul_wrong_id_param.xml` |
| 파라미터 부족 (이력) | `<LawSearch><target>…</target><totalCnt>0</totalCnt></LawSearch>` | **없음** | `error_dayjochg_id_only_zero.xml` |
| 미신청 target | **HTML 안내 페이지** | 해당 없음 | `error_target_lsHistory.html` |
| 알 수 없는 target | **본문 0바이트** | 해당 없음 | `error_unknown_target_empty.xml` |

`resultCode`는 **`lawSearch.do`의 정상 응답에만** 있다. 관측된 값은 `00`뿐이며, 다른 값은
확인하지 못했다 — **미확인**.

### 7.3 인코딩

XML 선언의 encoding 대소문자가 **API마다 다르다**. 실제 바이트는 모두 UTF-8이며 파싱에
문제는 없었다.

| 선언 | 해당 응답 |
|---|---|
| `encoding="UTF-8"` | lawSearch.do 전체, `target=law` 본문, 변경이력, admrul 본문 |
| `encoding="utf-8"` | `target=eflaw` 본문, lawService.do 실패 메시지 |

### 7.4 CDATA

`law_009244_mst252787.xml` 한 파일에 CDATA 블록 **1,126개**. 조문내용·항내용·호내용·목내용·
법령명이 모두 CDATA로 감싸여 있다. 본문 안의 `<개정 2020.3.24>` 같은 마커가 XML 태그로
오인되지 않는 것은 CDATA 덕분이다.

### 7.5 응답 지연과 안정성

호출 간 1.2초 간격으로 약 60회 호출, **실패·rate limit·타임아웃 0건**.

| 응답 크기 | 관측 지연 |
|---|---|
| 2 KB (목록) | 0.9 ~ 1.1초 |
| 130 KB (특금법 본문) | 2.1초 |
| 1.2 MB (소득세법) | 5.7초 |
| 2.1 MB (자본시장법) | 6.1초 |

---

## 8. 미확인 항목

확인하려면 무엇을 해야 하는지 함께 적는다.

| 항목 | 확인 방법 |
|---|---|
| `type=JSON` 응답 구조 | 같은 요청을 `type=JSON`으로 호출해 XML과 필드명·중첩을 대조 |
| `lawService.do target=law`의 `ID` 파라미터 | `ID=009244`로 본문 조회를 시도 (MST만 실측함) |
| `JO` 파라미터로 단일 조문 조회 | `target=law&MST=…&JO=000200` 호출 |
| `chrClsCd`(한글/원문) 동작 | `target=eflaw`에 `chrClsCd=010201`로 호출해 차이 확인 |
| `lsHstInf`의 `org` 필터 | 소관부처 코드와 함께 호출 |
| `lsJoHstInf`의 `fromRegDt`/`toRegDt` 기간 조회 | 기간을 주고 `regDt` 단건과 결과 비교 |
| `resultCode`의 `00` 이외 값 | 의도적으로 다양한 오류를 유발해 수집. 현재 관측값은 `00`뿐 |
| `lsHistory`(법령 연혁) 응답 구조 | **open.law.go.kr에서 해당 API 활용신청 필요** |
| `oldAndNew`(신구법 비교) 본문 구조 | 목록 조회는 동작 확인. 본문 조회를 호출해 스키마 확인 |
| `thdCmp`(3단 비교) 본문 구조 | 위와 동일 |
| 조문번호 4자리 초과 법령의 존재 | 조문 수가 가장 많은 법령을 찾아 확인 (관측 최대 449) |
| 가지번호 2자리 초과 사례 | 관측 최대 27. 반례를 찾으려면 가지번호가 많은 법령을 추가 조사 |
| 조문키 유형 자리의 `0`/`1` 외 값 | 더 많은 법령으로 확대 조사 |
| `일괄개정` 변경사유의 의미 | 해당 사례(2건)의 법령 전후 버전을 직접 대조 |
| 별표 첨부파일 링크의 실제 접근 가능성 | `별표서식파일링크`를 호출해 응답 확인 |
| 행정규칙의 조문 단위 제공 여부 | `조문형식여부=Y`인 다른 행정규칙도 동일한지 확인 |
| 행정규칙 변경이력 API 존재 여부 | 활용가이드의 행정규칙 항목 재확인 |
| API 호출 rate limit | 미확인. 1.2초 간격 60회에서는 제한에 걸리지 않았다 |
