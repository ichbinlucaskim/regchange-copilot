"""비밀값 조회 인터페이스. 자격증명이 코드와 로그에 남지 않게 한다.

목적:
    DB 자격증명, 외부 API 키 등 비밀값을 이름으로 조회하는 최소 경계를
    선언한다.

구현 이유:
    설정값 전체가 아니라 비밀값만 이 경계를 통과하게 한다. 일반 설정과 비밀값을
    같은 객체로 다루면 설정 덤프나 오류 리포트에 비밀값이 함께 실려 나간다.
    분리해 두면 "이 값은 로그에 찍으면 안 된다"가 타입에서 드러난다.

트레이드오프:
    설정 소스가 둘(환경변수 기반 설정, 비밀값 제공자)로 나뉘어 초기화 코드가
    늘어난다. 단일 설정 객체의 편의를 포기한 대신 유출 경로를 줄였다.

엣지 케이스:
    - 값의 캐싱 정책은 구현체가 정한다. 회전(rotation)이 있는 환경에서 무기한
      캐싱은 만료된 자격증명을 계속 쓰게 만든다.
    - 이 패키지의 어떤 코드도 비밀값을 로그에 남기지 않는다. 예외 메시지에도
      값이 아니라 이름만 넣는다.
"""

from typing import Protocol


class SecretProvider(Protocol):
    """이름으로 비밀값을 조회하는 경계.

    목적:
        실행 환경(로컬 .env, 운영 비밀 관리 서비스)에 무관하게 같은 방식으로
        자격증명을 얻는다.

    구현 이유:
        조회만 선언하고 저장·회전 메서드를 두지 않았다. 애플리케이션이 비밀값을
        쓸 수 있으면 유출 시 피해가 조회에서 끝나지 않는다. 회전은 운영 도구의
        책임으로 남긴다.

    트레이드오프:
        애플리케이션이 자체적으로 자격증명을 갱신할 수 없다. 자동화를 포기한
        대신 권한을 최소로 유지했다. 원칙 5와 같은 방향의 결정이다.

    엣지 케이스:
        - 이름이 없는 경우: None 이 아니라 예외다. 비밀값 부재는 정상 상태가
          아니며, None 을 반환하면 호출부가 빈 문자열로 접속을 시도한다.
    """

    def get(self, name: str) -> str:
        """비밀값을 반환한다. 없으면 예외를 던진다.

        목적:
            이름에 해당하는 자격증명 값을 얻는다.

        구현 이유:
            동기 메서드로 둔다. 비밀값은 프로세스 기동 시점에 한 번 읽는 것이
            정상 사용이며, 비동기로 만들면 호출부가 요청 처리 경로에서 매번
            읽는 설계를 유도한다.

        트레이드오프:
            원격 비밀 저장소를 쓰는 구현체는 블로킹 호출을 하게 된다. 기동
            시점에 한정된 비용이므로 감수한다.

        엣지 케이스:
            - 부재·권한 없음: 예외를 던지되 메시지에 값이 아니라 이름만 넣는다.
            - 빈 문자열이 저장된 경우: 부재와 동일하게 예외로 취급한다.
              빈 자격증명으로 접속을 시도하면 실패 원인이 인증 오류로 위장된다.
        """
        ...
