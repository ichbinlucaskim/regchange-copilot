"""작업 큐 인터페이스. 승인과 실행 사이의 비동기 경계를 표현한다.

목적:
    승인 기록(온라인 경로)과 실제 실행(dispatch 워커)을 큐로 분리하기 위한
    최소 인터페이스를 선언한다.

구현 이유:
    승인 API 응답 안에서 외부 발송을 수행하지 않기 위해 큐를 둔다. 같은 요청
    안에서 발송하면 재시도가 중복 발송이 되고, API 타임아웃이 "보냈는지 모르는
    상태"를 만든다. 큐를 두면 실행은 멱등 처리 가능한 별도 프로세스의 일이 된다.

트레이드오프:
    구성요소가 하나 늘고 "승인했는데 아직 안 나갔다" 구간이 생긴다. 즉시성을
    포기한 대신 중복 발송과 불확정 상태를 없앴다. 규제 대응은 초 단위 지연이
    문제되지 않는 워크로드다.

엣지 케이스:
    - 페이로드에 모델 출력이나 프롬프트를 담지 않는다. 승인 레코드 식별자만
      담는다 (원칙 5). 페이로드가 커지면 워커가 모델 출력을 신뢰하기 시작한다.
    - 큐 구현이 at-least-once 인 것을 전제한다. 정확히 한 번은 가정하지 않고,
      멱등성은 소비자가 보장한다.
"""

from collections.abc import Mapping
from typing import Protocol

type JobPayload = Mapping[str, str]
"""작업 페이로드. 승인 레코드 식별자 등 참조 값만 담는다(문자열 한정)."""


class JobQueue(Protocol):
    """작업 발행과 완료 통지를 위한 경계.

    목적:
        승인 이후 실행되어야 할 작업을 발행하고, 소비자가 처리 완료를 알린다.

    구현 이유:
        소비 루프(폴링/구독)는 이 Protocol 에 넣지 않았다. 구현 기술마다 형태가
        너무 다르고, 지금 고정하면 잘못된 추상화를 이후 단계가 떠안는다.
        확실히 필요한 두 동작만 선언한다.

    트레이드오프:
        소비 측 인터페이스가 없어 워커 구현이 어댑터 구현체에 직접 의존하게
        된다. 조기 추상화를 피한 대가이며, 소비 방식이 정해지는 시점에 확장한다.

    엣지 케이스:
        - enqueue 실패: 예외를 전파한다. 승인 트랜잭션과 발행이 함께 성공해야
          하며, 조용한 실패는 "승인됐지만 영원히 실행되지 않는" 상태를 만든다.
        - 중복 acknowledge: 오류가 아니라 무동작으로 처리한다.
    """

    async def enqueue(self, job_type: str, payload: JobPayload) -> str:
        """작업을 발행하고 작업 ID 를 반환한다.

        목적:
            승인된 작업을 실행 대기 상태로 만든다.

        구현 이유:
            작업 ID 를 반환해 감사 로그가 승인 레코드와 실행 작업을 연결할 수
            있게 한다. 연결이 없으면 "승인은 있는데 실행 기록이 없는" 상황의
            원인을 규명할 수 없다.

        트레이드오프:
            ID 채번을 큐 구현에 위임하므로 형식이 구현마다 다르다. 형식 통일을
            포기한 대신 어댑터를 단순하게 유지했다.

        엣지 케이스:
            - 같은 승인에 대한 중복 발행: 큐가 막지 않는다. 멱등성은 소비자가
              승인 레코드 상태로 보장한다.
        """
        ...

    async def acknowledge(self, job_id: str) -> None:
        """작업 처리 완료를 통지한다.

        목적:
            처리된 작업이 재전달되지 않게 한다.

        구현 이유:
            성공/실패를 인자로 받지 않는다. 실패는 acknowledge 하지 않는 것으로
            표현하며, 재시도 정책은 큐 구현의 책임으로 둔다.

        트레이드오프:
            실패 사유를 큐에 남기지 못한다. 사유는 감사 로그가 담당하므로
            중복해서 두지 않는다.

        엣지 케이스:
            - 존재하지 않거나 이미 처리된 작업 ID: 무동작. 예외를 던지면
              재시도 경로가 불필요하게 실패한다.
        """
        ...
