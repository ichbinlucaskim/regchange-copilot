"""FastAPI 진입점과 HTTP 경계를 담당하는 패키지.

목적:
    담당자가 제안을 조회하고 승인·반려하는 인터페이스, 그리고 헬스체크를
    제공한다. 승인 요청을 받아 그래프에 승인 이벤트를 전달하는 것이 핵심
    책임이다.

구현 이유:
    이 계층은 검증과 판정을 하지 않는다. 요청을 도메인 이벤트로 변환해 넘길
    뿐이다. API 가 승인 규칙을 알기 시작하면 규칙이 두 곳에 존재하게 되고, 다른
    진입점(배치, 큐 소비자)이 생겼을 때 한쪽만 갱신되어 우회 경로가 열린다.

트레이드오프:
    얇은 계층이므로 단순한 요청도 도메인 계층을 거쳐야 하고 왕복이 늘어난다.
    지역적 효율을 포기한 대신 규칙의 단일 소재지를 지켰다.

엣지 케이스:
    - LLM 이 관여하는 읽기 경로는 읽기 전용 커넥션을 사용한다 (원칙 5).
    - 승인 엔드포인트는 멱등이어야 한다. 중복 승인 요청이 두 건의 승인 레코드를
      만들면 발송 워커가 두 번 동작한다.
    - 인증 실패·권한 없음을 404 로 뭉뚱그리지 않는다. 감사 로그에서 접근 시도와
      부재를 구별해야 한다.
    - 예외를 500 으로 삼키지 않는다. 실패 사유를 구조화 로그에 남긴다.
"""
