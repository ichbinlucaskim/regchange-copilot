"""킬 스위치와 출력 스키마 강제 등 실행 전 차단 장치를 모으는 패키지.

목적:
    LLM 호출·검색·발송의 활성화 여부를 판정하는 킬 스위치, 모델 출력의 스키마
    강제, 인용 누락 시 폐기 규칙을 한곳에 모은다. "무엇을 하지 않을 것인가"를
    구현하는 패키지다.

구현 이유:
    차단 로직을 각 호출 지점에 흩어 두면, 새 호출 지점이 추가될 때 검사가 빠진
    경로가 생기고 그 사실이 리뷰에서 보이지 않는다. 한 패키지에 모아 두면
    "이 시스템의 안전장치 전부"를 한 파일 목록으로 감사에 제출할 수 있다.

트레이드오프:
    도메인 로직과 안전장치가 서로 다른 패키지에 있어 호출 흐름을 따라가려면
    파일을 오가야 한다. 응집도 대신 감사 가능성과 누락 방지를 택했다. 이
    시스템에서 안전장치의 목록화 가능성은 가독성보다 우선한다.

엣지 케이스:
    - 킬 스위치 기본값은 꺼짐이다 (fail-closed). 설정 누락, 조회 실패는 전부
      "꺼짐"으로 수렴한다. 설정 누락이 기능 활성으로 이어지면 아무도 그 상태를
      눈치채지 못한다.
    - 스위치가 꺼진 상태는 "결과 없음"과 구별되는 값으로 알린다 — **예외로 멈춘다**
      (`killswitch.KillSwitchError`). 빈 결과로 표현하면 비활성이 "영향 없음"으로
      오독된다.
    - 이 패키지는 스위치를 **켜는** API 를 제공하지 않는다. 읽고 판정만 한다.
      켜고 끄는 것은 `ops/switches.py` 이며 소유자 커넥션을 요구한다.

정정 이력 (2026-08-21, ADR-019):
    이 docstring 은 원래 "활성화는 배포 시점의 환경 설정으로만 이루어진다. 런타임
    토글은 감사 흔적 없이 경계를 연다"고 적었다. **전제가 틀렸다** — 런타임 토글이
    감사 흔적 없이 이루어질 이유가 없다. 지금은 상태가 bitemporal 테이블에 있고
    누가 언제 왜 바꿨는지가 행으로 남는다. 우려했던 것("흔적 없이 열린다")은
    저장 위치가 아니라 **기록의 유무**의 문제였다.

    반대로 배포 시점 설정을 고집했다면 요건 하나를 못 지켰다 — 장수 프로세스는
    재배포 없이 멈출 수 없다.
"""
