"""인용 검증을 생성과 분리된 경로에서 수행하는 패키지 (원칙 2, 3).

목적:
    초안이 제시한 인용이 (a) 실제 검색 결과 집합에 존재하는 문단 ID 인지,
    (b) 그 문단이 해당 주장을 실제로 뒷받침하는지를 판정한다.

구현 이유:
    두 검사를 분리한다. (a)는 집합 연산이므로 코드로 결정론적으로 처리하고,
    (b)만 모델에 맡긴다. 존재 여부까지 모델에게 물으면 존재하지 않는 ID 를
    "존재한다"고 답할 여지가 생기고, 그 순간 원칙 2의 강제력이 사라진다.
    또한 검증기는 생성기와 다른 호출로 실행한다. 같은 호출이 초안을 쓰고 스스로
    검증하면 검증은 형식화된다.

트레이드오프:
    모델 호출이 최소 두 번이 되어 지연과 비용이 늘고, 검증기가 지나치게 보수적일
    때 타당한 초안도 폐기된다. 처리량과 재현율을 포기한 대신, 통과한 출력에
    대해서는 근거가 실재한다는 보장을 얻었다. 이 도메인에서는 놓친 제안보다
    근거 없는 제안이 비싸므로 방향이 맞다.

엣지 케이스:
    - 인용 ID 가 검색 결과에 없는 경우: 경고 후 통과가 아니라 응답 전체를
      폐기한다. 일부만 제거하면 남은 문장이 사라진 근거를 참조하게 된다.
    - 인용이 0건인 경우: 스키마 위반으로 폐기한다. 인용은 optional 이 아니다.
    - 검증기 자체가 실패·타임아웃: 실패는 통과가 아니다. 폐기하고 재시도한다.
    - I/O 의존성을 갖지 않는다. 검색 결과와 초안을 인자로 받아 판정만 한다.
"""
