"""프롬프트 정의를 코드와 분리해 버전 관리하는 패키지 (원칙 3).

목적:
    초안 생성기와 인용 검증기의 프롬프트를 각각 정의하고, 버전을 부여해
    "어떤 프롬프트로 만든 출력인가"를 감사 로그에서 역추적할 수 있게 한다.

구현 이유:
    생성기와 검증기의 프롬프트를 같은 모듈에 두되 서로의 텍스트를 공유하지
    않는다. 공유하면 표현이 수렴하고, 검증기가 생성기의 관점을 그대로 물려받아
    같은 오류를 통과시킨다. 프롬프트를 코드 문자열이 아니라 버전 붙은 자원으로
    다루는 이유는, 출력 품질 변화의 원인이 모델 변경인지 프롬프트 변경인지
    구별해야 하기 때문이다.

트레이드오프:
    프롬프트를 고칠 때마다 버전을 올려야 하므로 반복 실험의 마찰이 커진다.
    실험 속도를 일부 포기한 대신 출력의 추적 가능성을 얻었다. 실험은 evals/
    에서 버전을 붙인 채로 돌리는 것으로 대체한다.

엣지 케이스:
    - 검증기 프롬프트는 생성기의 추론 과정이나 프롬프트를 입력으로 받지 않는다.
      출력 텍스트와 검색된 원문만 받는다.
    - 프롬프트에 규제 조항 번호를 예시로 넣지 않는다. 확인되지 않은 예시는
      모델이 그대로 인용해 사실처럼 출력한다 (CLAUDE.md §5.1).
    - 사용자·문서 텍스트는 지시가 아니라 데이터로 감싸 전달한다. 인젝션이
      성립하더라도 원칙 5(읽기 전용 role)로 피해를 조회 범위에 가둔다.
"""
