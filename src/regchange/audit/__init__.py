"""감사 로그 기록과 판단 근거 재현을 담당하는 패키지.

목적:
    어떤 입력·검색 결과·프롬프트 버전·모델 출력·검증 결과·승인 주체로 특정
    제안이 만들어졌는지를 기록하고, 사후에 그 시점의 판단 근거를 그대로 다시
    제시한다.

구현 이유:
    감사 대응의 단위는 "결론"이 아니라 "그 결론에 이른 경로"다. 최종 제안만
    남기면 6개월 뒤 같은 질문에 답할 수 없다. 재현에 필요한 것은 모델 출력이
    아니라 그 출력을 만든 입력 집합이므로, 검색 결과 문단 ID와 프롬프트 버전을
    함께 고정한다.

트레이드오프:
    기록량이 많아지고, 개인정보·민감정보가 로그에 섞이지 않도록 필드 단위로
    관리해야 한다. 저장 비용과 관리 부담을 감수한 대신 재현성을 얻었다.
    무엇을 남기지 않을지는 docs/03-data-classification.md 에서 정한다.

엣지 케이스:
    - 감사 레코드는 UPDATE 하지 않는다. 정정은 새 레코드 추가로 표현한다
      (원칙 6).
    - 로그 기록 실패: 조용히 넘어가지 않는다. 근거를 남기지 못한 실행은
      실행하지 않은 것과 같이 취급하는 편이 안전하다.
    - 프롬프트나 모델 출력에 원문 전체를 복사해 두지 않고 스냅샷 참조로 남긴다.
      복사본이 여럿이면 어느 것이 시스템이 실제로 본 원문인지 알 수 없다.
    - 재현 시점의 코드가 바뀐 경우: 코드 버전을 함께 기록해, 재현 결과가 다르면
      그 차이를 설명할 수 있게 한다.
"""
