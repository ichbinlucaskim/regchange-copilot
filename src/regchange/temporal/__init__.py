"""bitemporal 모델과 특정 시점의 지식 상태를 재구성하는 패키지 (원칙 6).

목적:
    모든 규제·정책 레코드에 대해 valid_time(현실에서 효력이 있었던 기간)과
    transaction_time(시스템이 그 사실을 알고 있었던 기간)을 다루고, 임의의
    과거 시점 기준으로 "그때 담당자가 알 수 있었던 정보"를 복원한다.

구현 이유:
    감사에서 받는 질문은 "지금 무엇이 맞는가"가 아니라 "그 시점에 무엇을 알 수
    있었는가"이다. 단일 시간축(현재 상태만 유지)으로는 이 질문에 답할 수 없다.
    시행일이 소급되거나 개정 사실을 뒤늦게 인지한 경우, 두 축이 없으면 사후에
    구성한 이야기와 당시의 판단 근거를 구별할 수 없다.

트레이드오프:
    모든 조회에 두 개의 시점 조건이 붙어 쿼리와 인덱스가 복잡해지고, 레코드가
    누적되므로 저장 비용이 단조 증가한다. 조회 단순성과 저장 효율을 포기한 대신
    감사 재현성을 얻었다. 이 시스템의 존재 이유가 감사 대응이므로 다른 선택지가
    없다.

엣지 케이스:
    - 소급 적용(시행일이 인지 시점보다 과거): valid_time 시작이
      transaction_time 시작보다 이르다. 정상 상태로 취급한다.
    - 정정 공고: 기존 레코드를 UPDATE 하지 않는다. transaction_time 을 닫고
      새 레코드를 연다.
    - 시점 조회에 naive datetime 이 들어오는 경우: 거부한다. 타임존 없는 시각은
      경계일 근처에서 조용히 다른 답을 낸다 (ruff DTZ 룰셋으로도 강제한다).
    - 두 축이 겹치는 복수 레코드: 최신 transaction_time 하나를 고르는 것이
      아니라, 겹침 자체를 데이터 오류로 보고한다.
"""
