"""평가 트리 루트 패키지.

목적:
    `evals.runners.*` 를 온전한 패키지 경로로 만든다. 러너끼리 서로를 import 할 수
    있어야 하고(적재 러너의 어댑터 조립을 채점 러너가 재사용한다), 그러려면 루트에
    `__init__.py` 가 있어야 한다.

구현 이유:
    이 파일이 없으면 mypy 가 같은 파일을 `runners.policy_index` 와
    `evals.runners.policy_index` 두 이름으로 보고 실패한다. `--explicit-package-bases`
    같은 설정으로 우회할 수도 있지만, 설정으로 가리는 대신 실제 패키지 구조를 맞춘다 —
    우회 설정은 왜 필요한지가 6개월 뒤에 남지 않는다.

트레이드오프:
    평가 트리가 import 가능한 패키지가 되므로, 실수로 `src/` 코드가 `evals` 를
    import 할 수 있게 된다. 그 방향은 의존 역전이며 import-linter 계약으로 막는다.

엣지 케이스:
    - 이 패키지는 아무것도 재수출하지 않는다. 러너는 각자 `__main__` 진입점을 가진
      실행 단위이며, 여기서 모아 노출하면 import 만으로 부수효과가 생길 여지가 있다.
"""
