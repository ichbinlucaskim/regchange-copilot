"""평가 러너 패키지.

목적:
    evals/datasets 아래 데이터셋으로 파이프라인을 실행하고, 결과를
    evals/results 에 재현 가능한 형태로 남긴다.

구현 이유:
    평가를 tests/ 가 아니라 별도 트리에 둔다. 단위 테스트는 결정론적이고 빨라야
    하며 CI 에서 항상 돈다. 평가는 실제 모델을 호출하므로 느리고 비결정적이고
    비용이 든다. 한 트리에 섞으면 둘 중 하나의 기준이 무너진다.

트레이드오프:
    같은 코드에 대해 두 개의 실행 경로를 유지해야 한다. 중복을 감수한 대신
    "CI 는 항상 초록"과 "평가는 실제 모델로"를 동시에 지켰다.

엣지 케이스:
    - 결과 파일은 커밋하지 않는다(.gitignore). 커밋 대상은 러너와 데이터셋이며,
      결과는 그 둘로부터 재생산되어야 한다.
    - 실제 고객 데이터를 데이터셋에 넣지 않는다 (CLAUDE.md §5.2).
    - 데이터셋이 비어 있으면 성공이 아니라 실패로 보고한다. 0건 평가가 통과로
      기록되면 회귀를 놓친다.
"""
